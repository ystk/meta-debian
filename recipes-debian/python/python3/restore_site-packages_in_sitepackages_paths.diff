Debian has removed /usr/lib/python*/site-packages,
but there are still python modules are installed into that directory.

diff --git a/Lib/site.py b/Lib/site.py
index 88c8046..831afa2 100644
--- a/Lib/site.py
+++ b/Lib/site.py
@@ -351,6 +351,9 @@ def getsitepackages(prefixes=None):
                                              "python3",
                                              "dist-packages"))
             # this one is deprecated for Debian
+            sitepackages.append(os.path.join(prefix, sys.lib,
+                                        "python%d.%d" % sys.version_info[:2],
+                                        "site-packages"))
             sitepackages.append(os.path.join(prefix, sys.lib,
                                              "python" + sys.version[:3],
                                              "dist-packages"))

--- a/Lib/test/test_site.py
+++ b/Lib/test/test_site.py
@@ -269,7 +269,7 @@ class HelperFunctionsTests(unittest.Test
         dirs = site.getsitepackages()
         if os.sep == '/':
             # OS X, Linux, FreeBSD, etc
-            self.assertEqual(len(dirs), 3)
+            self.assertEqual(len(dirs), 4)
             wanted = os.path.join('xoxo', 'local', 'lib',
                                   'python%d.%d' % sys.version_info[:2],
                                   'dist-packages')
